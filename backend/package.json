{
  "name": "pay-on-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API for PAY-ON using Express and PostgreSQL with Prisma.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:validate": "prisma validate",
    "prisma:studio": "prisma studio",
    "lint": "eslint ."
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.16.2"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "prisma": "^5.20.0"
  }
}
